const initialState = {
  name: '',
  assertions: '',
  score: '',
  gravatarEmail: '',
};

const player = (state = initialState, action) => {
  switch (action) {
  default:
    return state;
  }
};

export default player;
